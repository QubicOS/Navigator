import { Theme } from "../theme.slint";

export component SettingsApp inherits Rectangle {
    background: #00000000;

    Text {
        x: Theme.gutter;
        y: Theme.gutter;
        text: "Настройки (пока заглушка)";
        color: Theme.text_dim;
        font-size: Theme.font_md;
        font-weight: 600;
    }
}

